import 'package:flutter/material.dart';

class TPRSTab extends StatelessWidget {
  const TPRSTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
