import 'package:flutter/material.dart';

class FiliaisTab extends StatelessWidget {
  const FiliaisTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
