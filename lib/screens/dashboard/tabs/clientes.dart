import 'package:flutter/material.dart';

class ClientesTab extends StatelessWidget {
  const ClientesTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
