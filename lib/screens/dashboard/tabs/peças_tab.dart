import 'package:flutter/material.dart';

class PecasTab extends StatelessWidget {
  const PecasTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
