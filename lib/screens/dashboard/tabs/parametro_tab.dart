import 'package:flutter/material.dart';

class ParametroTab extends StatelessWidget {
  const ParametroTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
